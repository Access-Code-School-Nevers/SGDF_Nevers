{% extends 'base.html.twig' %}

{% block title %}Retrait{% endblock %}

{% block body %}

<section class="support_view">
    <div class="container_view">
      <div class="mt-3 flex-height d-flex flex-column justify-content-center">
        <div class="content_restituer_articles">
          <div class="info_articles p-3">
            <p><i class="fas fa-quote-right mr-1"></i>Scannez vos articles ou rentrez manuellement le code à chiffre (sous le code barre), puis attendez la confirmation.</p>
          </div>
          <!-- List of articles to withdraw -->
          <form class="w-100" method="POST">

          <div class="content_restituer_list_articles">
            <div class="table_container mt-5">
              <div class="table_container_color w-100 height_table_historique">
                <table class="table_articles">
                  <thead class="head_table_resistuer">
                    <tr>
                      <th>Articles à retirer</th>
                    </tr>
                  </thead>
                  <tbody class="body_table_restituer">
                    {% set barCodeId = 1 %} <!--  Used to list the codeBar naes -->

                    {% for keyEmplacement, emplacement in objectsByEmplacement %}
                      {% for object in emplacement %}
                        <tr class="bar-default" id="el{{ barCodeId }}"><!-- rajouter la class "bar-success" lors de la validation -->
                            <th class="plr-6">
                              <div class="scan-in-progress"> <!-- par default "display: flex;" || rajouter la class "d-none" lors de la validation -->
                                <i class="fas fa-boxes"></i>
                                <div class="">
                                  <p>Article{% if object.listArticles|length > 1 %}(s){% endif %}<strong>id</strong></p>
                                  {% for article in object.listArticles %}
                                    <p>{{ object.titre }}<strong>{{ article }}</strong></p>
                                  {% endfor %}
                                </div>
                                  <p>Quantité <span>{{ object.listArticles|length }}</span></p>
                                <div class="scan_articles_resituer">
                                  <input id="codebar{{ barCodeId }}" data-id-required="{{ keyEmplacement }}" class="input_style_restituer" type="text" name="codebar{{ barCodeId }}" placeholder="CAB à scan : {{ keyEmplacement }}" oninput="verifyScan(this, {{ barCodeId }})" required>
                                  <a onClick="initScan({{ barCodeId }})" class="button-scan input_button">
                                      <div class="button_scan_restituer m-0"></div>
                                  </a>
                                </div>
                                <div class="check-box-success">
                                </div>
                              </div>
                              <div class="d-none scan-success"> <!-- par default en display: none;  || enlever la class "d-none" lors de la validation -->
                                <p>Code-barres scanné avec succès!</p>
                                <button type="button" name="button" class="button_success button_style">ok</button>
                                <div class="check-box-success">
                                  <i class="fas fa-check check-success"></i>
                                </div>
                              </div>
                          </th>
                        </tr>
                      {% endfor %}
                      {% set barCodeId = barCodeId + 1 %}
                    {% endfor %}

                  </tbody>
                  <tfoot class="position-relative">
                    <tr>
                      <th scope="row"><div class="button_confirm_restituer">
                        <input class="button_style w-150px submit_search_article color_button" type="submit" name="Envoyer"></input>
                      </div></th>
                   </tr>
                  </tfoot>
                </table>
              </div>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Scanner -->
  <div id="barcode-scanner">
    <video src=""></video>
    <canvas class="drawingBuffer"></canvas>
  </div>
</section>
<script src="/js/scan_articles.js"></script>
{% include 'components/footer.html.twig' %}
{% endblock %}
